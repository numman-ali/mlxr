version: 1
display_name: Validation
short_description: Runtime debugging, logs, and benchmark-discipline workflow for MLXR.
default_prompt: Use the validation workflow to inspect runtime logs, benchmark strength, and whether a result is strong enough to treat as repo truth.
