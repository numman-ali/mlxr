[project]
name = "mlx-runtime-workflows"
version = "0.1.0"
description = "Workflow planning substrate for MLXR"
requires-python = ">=3.12"
dependencies = [
    "mlx-runtime-schemas",
    "pydantic>=2.10.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
