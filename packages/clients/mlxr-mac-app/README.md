# mlxr-mac-app

This directory will become the first-party native macOS client for `MLXR`.

It is meant to be:

- a simple Swift-native app
- a thin client over the shared runtime
- separate from the `ltx-desktop` compatibility adapter

The first app is intentionally smaller than the longer-term `MLXR Studio`
product vision.
