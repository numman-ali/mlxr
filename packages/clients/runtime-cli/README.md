# runtime-cli

This package will become the first-party thin client over the local daemon.

Planned command groups:

- `mlx-runtime serve`
- `mlx-runtime models *`
- `mlx-runtime run`
- `mlx-runtime jobs *`
