# adapter-ltx-desktop

This package will become the thin runtime client for `ltx-desktop`.

The goal is to preserve the current product shell while replacing direct local Torch execution with calls to the shared runtime daemon.
