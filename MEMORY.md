# MEMORY.md

This file is the durable cross-session memory for `MLXR`.

Use it to preserve stable repo-operating learnings so fresh Codex sessions do not have to rediscover them.

## How To Use This File

### Tier 1 — Must Read

- Read this section at the start of every session, immediately after `AGENTS.md`.
- Keep it short and durable.
- Promote only facts that should shape most future sessions.

### Tier 2 — Lookup Log

- Do not read this end to end by default.
- Search it when working on a related area.
- Append durable learnings here at the end of a session.

## Memory Update Rule

- Add durable learnings to Tier 2.
- Promote only cross-session critical items to Tier 1.
- Keep Tier 1 focused on durable repo facts and sharp edges, not doctrine already stated in `AGENTS.md`.
- If a rule belongs in repo operating doctrine, put it in `AGENTS.md` instead of repeating it here.
- Archive obsolete items instead of silently deleting them.
- Do not put transient task notes or diary-style entries here.

## Tier 1 — Must Read

- The default local quality gate is `uv run python scripts/dev.py verify`.
- `pre-commit` is a fast hygiene mirror, not the main acceptance gate.
- If runtime behavior changes, inspect `$MLX_RUNTIME_HOME/logs/control-plane.log` as part of validation.
- `verify` and `pre-commit` run `scripts/check_type_escapes.py`; avoid `typing.cast` and explicit `Any` in the scanned runtime and test surfaces.
- Placeholder package folders stay outside the `uv` workspace until they gain a real `pyproject.toml`.
- Python imports now use the shared `mlxr.*` namespace; run type checks through `scripts/run_mypy.py` because namespace-package repos need package-targeted mypy invocation rather than a single flat file crawl.

## Tier 2 — Lookup Log

- (2026-03-06) [HARNESS] The repo uses explicit `uv` workspace membership and `tool.uv.sources`; root `uv sync`, `uv lock`, and `uv run` should work without extra flags. — refs: `pyproject.toml`, `uv.lock`
- (2026-03-06) [QUALITY] The current local quality stack is `ruff`, `mypy --strict`, `unittest`, and `uv build --all-packages`. — refs: `pyproject.toml`, `scripts/dev.py`
- (2026-03-06) [LOGS] The control plane logs to `$MLX_RUNTIME_HOME/logs/control-plane.log`, and runtime log inspection is part of the default dev loop. — refs: `packages/core/runtime-server/src/mlxr/core/server/logging.py`, `docs/dev-harness.md`
- (2026-03-06) [SKILLS] The repo-owned specialized overlays live under `.agents/skills/`: `validation` for generic runtime validation and `ltx-fidelity-debugging` for real-weight LTX fidelity bring-up. They are narrow by design and not replacements for the common startup flow. `.codex/` is reserved for project-local Codex config, not skill bodies. — refs: `.agents/skills/validation/SKILL.md`, `.agents/skills/ltx-fidelity-debugging/SKILL.md`, `AGENTS.md`
- (2026-03-06) [COVERAGE] `verify` enforces an `85%` package coverage floor via `coverage.py`; the floor applies to `packages/`, not repo scripts. — refs: `pyproject.toml`, `scripts/dev.py`
- (2026-03-06) [INSPECTION] Family preflight inspection now runs from `ResolvedSource` without provider fetch; selective provider fetch happens at conversion time through the family adapter’s fetch-policy seam. Hugging Face provenance also preserves `remote_code_approved` from source policy. — refs: `packages/core/runtime-core/src/mlxr/core/runtime/catalog.py`, `packages/core/runtime-core/src/mlxr/core/runtime/contracts.py`, `packages/core/runtime-core/src/mlxr/core/runtime/providers.py`, `packages/families/ltx/src/mlxr/families/ltx/adapter.py`
- (2026-03-06) [ARTIFACTS] LTX Phase B now uses a family-generic multi-source conversion contract: `ArtifactConversionRequest` accepts exactly one of `source_id` or `source_bindings`, portable artifacts can carry typed `components`, and the first truthful LTX slice materializes copied payloads for `checkpoint`, `spatial_upsampler`, and `text_encoder` under `artifacts-portable/.../payload/`. — refs: `packages/core/shared-schemas/src/mlxr/core/schemas/models.py`, `packages/core/runtime-core/src/mlxr/core/runtime/catalog.py`, `packages/families/ltx/src/mlxr/families/ltx/adapter.py`, `docs/research/07-ltx-integration-seams.md`
- (2026-03-06) [PROMPT-ENCODE] The next truthful LTX execution seam is now real: artifactized LTX models lazily create a strict-local MLX Gemma prompt encoder at `prompt_encode`, store prompt context in worker-local runtime state, and keep denoise/generate scaffolded until a later slice. Job inputs with non-empty `negative_prompt` now fail clearly for the fast path instead of being ignored. — refs: `packages/families/ltx/src/mlxr/families/ltx/prompt_encoding.py`, `packages/families/ltx/src/mlxr/families/ltx/adapter.py`, `packages/families/ltx/README.md`
- (2026-03-06) [PROMPT-ENCODE-HARNESS] The typed public prompt-encode seam now lives in `prompt_encoding.py`, while the large MLX implementation is isolated in `_prompt_encoding_backend.py` and omitted from repo coverage until it has real asset-backed tests. API job tests patch `mlxr.core.server.jobs._job_process_context()` to an in-process threaded context so prompt-encoder patches survive worker execution without changing production spawn behavior. — refs: `packages/families/ltx/src/mlxr/families/ltx/prompt_encoding.py`, `packages/families/ltx/src/mlxr/families/ltx/_prompt_encoding_backend.py`, `tests/runtime_test_support.py`, `pyproject.toml`
- (2026-03-06) [LTX-EXECUTION] The first non-placeholder LTX runtime path now resolves imported image handles inside the worker, emits per-stage timing plus MLX memory telemetry, narrows truthful fast-path outputs to runtime-managed `mp4` video, and generates encoded preview video artifacts without claiming checkpoint-faithful denoise yet. — refs: `packages/families/ltx/src/mlxr/families/ltx/adapter.py`, `packages/families/ltx/src/mlxr/families/ltx/generation.py`, `packages/core/runtime-server/src/mlxr/core/server/worker.py`, `docs/research/07-ltx-integration-seams.md`
- (2026-03-06) [HF-BENCHMARK] Hugging Face-backed benchmark runs should isolate download caches with `HF_HUB_CACHE` and `HF_XET_CACHE` instead of overriding `HF_HOME`; `HF_HOME` also carries the saved `hf auth login` token, so replacing it can break gated-model access. On macOS, benchmark UDS sockets should use a short explicit path because deep runtime-home paths can exceed the `AF_UNIX` limit. — refs: `packages/clients/runtime-cli/src/mlxr/clients/cli/benchmark_ltx.py`, `packages/core/runtime-server/src/mlxr/core/server/__main__.py`, `benchmarks/README.md`
- (2026-03-06) [LTX-PROMPT-V2] The current upstream `LTX-2.3` checkpoint uses the Gemma prompt V2 path, not the old single `aggregate_embed` path. The repo-owned prompt backend now needs to read checkpoint config, load `video_aggregate_embed` / `audio_aggregate_embed`, and honor the current connector config (`32x128` video heads, `32x64` audio heads, `8` layers, split rope, float64 rope, gated attention). — refs: `packages/families/ltx/src/mlxr/families/ltx/_prompt_encoding_backend.py`, `references/official/LTX-2/packages/ltx-core/src/ltx_core/text_encoders/gemma/encoders/encoder_configurator.py`, `references/official/LTX-2/packages/ltx-core/src/ltx_core/text_encoders/gemma/embeddings_connector.py`
- (2026-03-07) [LTX-GENERATION-2.3] The current `LTX-2.3` bridge cannot rely on the ecosystem `mlx-video` defaults as-is. The repo-owned generation path now patches the donor transformer for `caption_proj_before_connector`, gated attention, and cross-attention AdaLN, and it uses config-driven local loaders for the video decoder and spatial upsampler because the upstream `2.3` checkpoint metadata carries a newer 9-block VAE decoder shape and an `upsampler.0.*` naming layout the donor loaders do not fully match. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend.py`, `references/ecosystem/mlx-video/mlx_video/models/ltx/ltx.py`, `references/ecosystem/mlx-video/mlx_video/models/ltx/video_vae/decoder.py`, `references/ecosystem/mlx-video/mlx_video/models/ltx/upsampler.py`
- (2026-03-07) [LTX-REAL-SMOKE] The truthful distilled two-stage `LTX-2.3` fast path now completes direct artifact-backed T2V and I2V smoke runs against real upstream weights. The remaining execution gap is no longer “preview versus real generation”; it is recommended-resolution, HQ-profile, and broader feature validation. Because the MLX generation bridge is large and asset-backed, `_generation_backend.py` is temporarily omitted from coverage like `_prompt_encoding_backend.py` until it has dedicated asset-backed test coverage. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend.py`, `packages/families/ltx/README.md`, `pyproject.toml`
- (2026-03-07) [LTX-FIDELITY-GUARDRAILS] The `LTX-2.3` bridge now treats prompt-to-transformer shape/config drift, 22B gated-attention and cross-attention AdaLN feature flags, VAE `mean-of-means` / `std-of-means`, and x2 upsampler key-layout support as fail-closed compatibility boundaries. The prompt encoder now prefers dedicated connector metadata when present and returns an explicit post-connector contract so generation stops silently reshaping mismatched context. — refs: `packages/families/ltx/src/mlxr/families/ltx/prompt_encoding.py`, `packages/families/ltx/src/mlxr/families/ltx/_prompt_encoding_backend.py`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend.py`, `tests/test_runtime_units.py`
- (2026-03-07) [LTX-AV-SMOKE-SAFETY] Real local `LTX-2.3` AV smokes on a `64 GB` Mac can spike into watchdog-danger territory if the Gemma prompt encoder stays resident while the 22B audio-video transformer loads. For manual diagnostics, encode the prompt, close the prompt encoder, clear cache, and only then load/generate. If a manual smoke is interrupted, check for orphaned `uv run python` / `scripts/ltx_debug_smoke.py` processes before launching another run. — refs: `packages/families/ltx/src/mlxr/families/ltx/adapter.py`, `scripts/ltx_debug_smoke.py`, `/Library/Logs/DiagnosticReports/Retired/panic-full-2026-03-07-145930.0002.panic`
- (2026-03-07) [LTX-FIDELITY-WORKFLOW] The canonical LTX fidelity-debug method is now `scripts/ltx_debug_smoke.py` plus `docs/research/10-ltx-fidelity-debug-playbook.md` and `.agents/skills/ltx-fidelity-debugging/`. Use fixed-seed real-weight runs, clean lifecycle, detached execution for long smokes, and stage-local `stage1` / `post_x2` / `final` snapshots to decide the next suspect. `256x160 / 17f` is the first meaningful semantic gate, and an ambiguous quadruped does not count as dog success. — refs: `scripts/ltx_debug_smoke.py`, `docs/research/10-ltx-fidelity-debug-playbook.md`, `.agents/skills/ltx-fidelity-debugging/SKILL.md`
- (2026-03-07) [LTX-DOG-LADDER] After restoring AV cross-timestep prep to use `modality.timesteps` and tightening the prompt-side audio fail-closed path, the canonical fixed-seed dog prompt now clears the first visual gates on the real Hugging Face-converted artifact root: `384x224 / 17f / 24fps` produces a clear dog in a park, and `768x512 / 33f / 24fps` produces a coherent dog scene across `frame_0001`, `frame_mid`, and `frame_last`. The canonical review bundles are under `tmp/manual-runs/20260307T174355Z-dog-visual-gate-after-av-stage1-fix/` and `tmp/manual-runs/20260307T174542Z-dog-coherence-after-av-stage1-fix/`. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend.py`, `packages/families/ltx/src/mlxr/families/ltx/_prompt_encoding_backend.py`, `tmp/manual-runs/20260307T174355Z-dog-visual-gate-after-av-stage1-fix/run_manifest.json`, `tmp/manual-runs/20260307T174542Z-dog-coherence-after-av-stage1-fix/run_manifest.json`
- (2026-03-07) [LTX-AUDIO-EXPORT] The real `LTX-2.3` checkpoint currently used by `MLXR` already carries `audio_vae.*` and `vocoder.*` weights inside the core checkpoint payload. The repo-owned AV bridge now decodes that audio branch and exports it as muxed `mp4` or standalone `wav`, and the first truthful `video.condition.audio` slice now preserves a resolved runtime audio handle through the output path instead of silently ignoring it. The authoritative surface map lives in `docs/families/ltx-capability-matrix.md`. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend.py`, `packages/families/ltx/src/mlxr/families/ltx/adapter.py`, `packages/families/ltx/src/mlxr/families/ltx/workflows.py`, `docs/families/ltx-capability-matrix.md`
- (2026-03-08) [LTX-AUDIO-FIDELITY] The real `LTX-2.3` checkpoint now runs through a repo-owned MLX BWE wrapper on top of the `AMP1` / `SnakeBeta` / no-tanh / no-bias base vocoder. The wrapper must treat repo-owned `AudioVocoder` waveforms as `[batch, channels, time]`, not `[batch, time, channels]`, and it must load the checkpoint's `vocoder.bwe_generator.*`, `vocoder.mel_stft.mel_basis`, and `vocoder.mel_stft.stft_fn.*` buffers fail-closed. The first BWE-enabled visual-gate receipt is `tmp/manual-runs/20260308T014947Z-dog-bwe-visual-gate-check/`. — refs: `packages/families/ltx/src/mlxr/families/ltx/_audio_bwe.py`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/video_stack.py`, `docs/families/ltx-capability-matrix.md`
- (2026-03-08) [SHOWCASE-GATING] `scripts/ltx_showcase_generate.py` is now the repo-owned showcase runner. Historical showcase receipts from this period used repo-owned prompt-shaping experiments plus stronger Gemini review gating and saved `ffprobe.json` receipts. The first 10-second natural-audio dog showcase receipt still matters as scene-level evidence, but not as proof that the current pass-through prompt contract solves natural-scene realism broadly. — refs: `scripts/ltx_showcase_generate.py`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/sampling.py`, `docs/research/12-ltx-prompting-guide.md`, `docs/research/13-ltx-showcase-execution-plan.md`, `tmp/showcase-runs/20260308T034004Z-showcase-dog-park-natural/gemini-review/gemini-review.json`
- (2026-03-08) [GEMINI-AUDIO-REVIEW] `scripts/gemini_describe_video.py` now stages a review WAV alongside the MP4 when the clip has audio and asks Gemini to review both in one XML response. The stronger review path is good enough to catch real semantic failures and real improvements: it previously confirmed music-like drift on the early dog, nature, and vintage natural-scene receipts, and it now confirms the improved 10-second dog receipt as a visual/audio match. Use the exact receipt path in repo truth instead of assuming one natural-scene result generalizes to the rest. — refs: `scripts/gemini_describe_video.py`, `docs/research/10-ltx-fidelity-debug-playbook.md`, `tmp/showcase-runs/20260308T022707Z-showcase-dog-park-natural/gemini-review-audio-aware/gemini-review.json`, `tmp/showcase-runs/20260308T024917Z-showcase-nature-documentary/gemini-review-audio-aware/gemini-review.json`, `tmp/showcase-runs/20260308T025209Z-showcase-vintage-old-school/gemini-review-audio-aware/gemini-review.json`, `tmp/showcase-runs/20260308T034004Z-showcase-dog-park-natural/gemini-review/gemini-review.json`
- (2026-03-08) [NEGATIVE-GUIDANCE] The earlier text-first natural-audio experiment stayed family-local to LTX: the prompt path carried positive and negative contexts together, and the distilled AV path temporarily applied rescaled CFG separately to video and audio. That slice cleared the historical 10-second dog natural-audio receipt, but as of `2026-03-09` the promoted owned distilled product path defaults back to positive-only denoising because the CFG slice did not generalize cleanly. Keep any distilled CFG use family-local and diagnostic-only. — refs: `packages/families/ltx/src/mlxr/families/ltx/_prompt_encoding_backend/encoder.py`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/sampling.py`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/distilled.py`, `tmp/showcase-runs/20260308T033618Z-showcase-dog-park-natural/run_manifest.json`, `tmp/showcase-runs/20260308T034004Z-showcase-dog-park-natural/run_manifest.json`
- (2026-03-08) [NATURAL-AUDIO-SCOPE] The newer negative-guidance/rescaled-CFG path is not a blanket fix for natural-audio realism. It genuinely recovers the 10-second dog scene, but the rerun `nature_documentary` and `vintage_old_school` scenes on the same path still come back as visual matches with music-like audio. Treat the current public story as the best-available mixed five-pack, not as solved natural-scene realism. — refs: `tmp/showcase-runs/20260308T034004Z-showcase-dog-park-natural/gemini-review/gemini-review.json`, `tmp/showcase-runs/20260308T035528Z-showcase-nature-documentary/gemini-review/gemini-review.json`, `tmp/showcase-runs/20260308T035838Z-showcase-vintage-old-school/gemini-review/gemini-review.json`, `docs/research/14-first-showcase-pack.md`
- (2026-03-08) [SCENARIO-AUDIO-SHAPING] Historical showcase receipts from this runner-only slice fed fixture-level `audio_intent` and style-aware anti-music hints into prompt-shaping experiments. Those receipts remain useful evidence about scene sensitivity, but they are not the current prompt contract after the 2026-03-09 pass-through cleanup. — refs: `scripts/ltx_showcase_generate.py`, `tests/test_ltx_showcase_generate.py`, `tmp/showcase-runs/showcase-summary-20260308T092838Z.json`, `tmp/showcase-runs/showcase-summary-20260308T093536Z.json`, `tmp/showcase-runs/showcase-summary-20260308T094420Z.json`, `tmp/showcase-runs/showcase-summary-20260308T094012Z.json`, `tmp/showcase-runs/showcase-summary-20260308T094732Z.json`
- (2026-03-08) [COMBINED-A2V-I2V] The current `ltx` workflow strategy already preserves image references when `video.condition.audio` is selected, which matches the official upstream direction for combined text + image + audio inputs. This combined slice is now pinned by runtime workflow tests and is a valid next promotion target after conditioned-scene quality improves. — refs: `packages/families/ltx/src/mlxr/families/ltx/workflows.py`, `tests/test_runtime_workflows.py`, `references/official/LTX-2/packages/ltx-pipelines/src/ltx_pipelines/a2vid_two_stage.py`
- (2026-03-09) [A2VID-DOG-SEMANTIC-PASS] The preserved-reference `video.condition.audio` row now has a real semantic pass through the normal `mlxr generate` product path: `tmp/manual-runs/20260309T082500Z-natural-audio-dog-6s-a2vid-better-image/` clears strict Gemini review as a golden-retriever-like dog running through a grassy park with barking and no music. The earlier mismatch was partly caused by a poisoned bark fixture: the old `dog_bark_ref.wav` was actually upbeat vocal music. For conditioned-audio validation, use a real bark reference plus a non-tiny dog image anchor; the postage-stamp `64x48` dog image is not a trustworthy semantic fixture. — refs: `tmp/manual-runs/20260309T082500Z-natural-audio-dog-6s-a2vid-better-image/natural_audio_dog_384x224_145f.mp4`, `tmp/gemini-reviews/20260309T082500Z-natural-audio-dog-6s-a2vid-better-image-review/gemini-review.json`, `tmp/audio-fixtures/dog_bark_real.wav`
- (2026-03-09) [TEXT-FIRST-QUIET-DRIFT] Historical real-CLI receipts before the pass-through prompt cleanup showed broad quiet-scene drift: after fixing prompt-hashed seeds and returning the default distilled runtime to positive-only denoising, `bookshop_dialogue_quiet`, `cafe_sidewalk_human`, and an audio-details-only bookshop rerun at `384x224 / 145f / 24fps / seed=17` all still collapsed into the wrong vintage/outdoor/music basin. Treat these as historical evidence from the old shaped-prompt contract, not as the current post-cleanup prompt path. — refs: `/Users/numman/.mlx-runtime/jobs/job_7ec83ccb4014459999f2023678e35d43/record.json`, `/Users/numman/.mlx-runtime/jobs/job_170385a46603438f9cdb188e03be33e1/record.json`, `/Users/numman/.mlx-runtime/jobs/job_777c5cee9dd841e495d1e736fa6bd06e/record.json`, `tmp/manual-runs/20260309T120000Z-bookshop-cli-post-positive-only-seed17/bookshop_384x224_145f.mp4`, `tmp/manual-runs/20260309T120800Z-cafe-cli-post-positive-only-seed17/cafe_384x224_145f.mp4`, `tmp/manual-runs/20260309T121400Z-bookshop-cli-audio-only-seed17/bookshop_384x224_145f.mp4`, `tmp/gemini-reviews/20260309T120000Z-bookshop-cli-post-positive-only-seed17-review/gemini-review.json`, `tmp/gemini-reviews/20260309T120800Z-cafe-cli-post-positive-only-seed17-review/gemini-review.json`, `tmp/gemini-reviews/20260309T121400Z-bookshop-cli-audio-only-seed17-review/gemini-review.json`
- (2026-03-09) [GEMMA-PROMPT-LOADER] The owned Gemma prompt stack has two sharp fail-closed requirements now pinned by tests: respect explicit `layer_types` instead of reconstructing sliding/global attention from a derived modulus, and register/load `embed_tokens.weight` directly so checkpoint key `language_model.model.embed_tokens.weight` cannot be silently skipped. The LTX prompt loader now checks post-load Gemma parameter coverage instead of trusting `strict=False` alone. — refs: `packages/core/runtime-mlx-models/src/mlxr/core/mlx_models/gemma3_text.py`, `packages/families/ltx/src/mlxr/families/ltx/_prompt_encoding_backend/components.py`, `packages/core/runtime-mlx-models/tests/unit/test_gemma3_text.py`, `packages/families/ltx/tests/unit/prompt/test_language_model_loading.py`
- (2026-03-09) [QUIET-TEXT-FIRST-MIXED-RECHECK] After the Gemma prompt-loader fix and before the pass-through prompt cleanup, a fresh exact-prompt `bookshop_dialogue_quiet` rerun through the real `mlxr` path matched semantically, while `cafe_sidewalk_human` matched visually but still reviewed as music-bearing. This narrowed the problem from total quiet-scene collapse to a mix of prompt-path interference and broader text-first audio instability. Treat both receipts as historical because the promoted runtime path now passes prompts through verbatim. — refs: `/Users/numman/.mlx-runtime/jobs/job_6b27c3252a544262a6ef3475998ffcea/record.json`, `/Users/numman/.mlx-runtime/jobs/job_af91d136aa0441c8acfd46abc5b1ee43/record.json`, `tmp/gemini-reviews/20260309T145200Z-bookshop-cli-post-gemma-fix-exact-seed17-review/gemini-review.json`, `tmp/gemini-reviews/20260309T145000Z-cafe-cli-post-gemma-fix-seed17-review/gemini-review.json`
- (2026-03-09) [PROMPT-PASSTHROUGH] The promoted runtime path now passes caller-authored prompts through verbatim. Shared workflow prompt-authoring fields and first-party CLI flags for prompt composition were removed, the LTX workflow no longer synthesizes negative prompts or extra prompt lines, and the showcase runner now sends scenario prompts through unchanged. If extra wording is desired, it must be authored by the caller explicitly. — refs: `packages/core/shared-schemas/src/mlxr/core/schemas/workflows.py`, `packages/clients/runtime-cli/src/mlxr/clients/cli/cli.py`, `packages/families/ltx/src/mlxr/families/ltx/workflows.py`, `scripts/ltx_showcase_generate.py`, `docs/research/12-ltx-prompting-guide.md`
- (2026-03-08) [MP4-EXPORT] The LTX MP4 export path now defaults to H.264 instead of legacy `mpeg4`. Prefer `h264_videotoolbox` on Apple Silicon, fall back to `libx264`, keep `yuv420p`, and mux AAC audio into the final container. The tiny runtime receipt `tmp/manual-runs/20260308T073808Z-h264-export-smoke/` is the first explicit proof that the current runtime-managed output now lands as `codec_name=h264` with `codec_tag_string=avc1`. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/outputs.py`, `tests/test_runtime_units.py`, `tmp/manual-runs/20260308T073808Z-h264-export-smoke/run_manifest.json`
- (2026-03-08) [PROMPT-SCENARIOS] The canonical reusable LTX prompt scenario pack now lives in `tests/fixtures/ltx/prompt_scenarios.json`, and `scripts/ltx_showcase_generate.py` loads that fixture instead of maintaining a second hardcoded scene table. Treat the fixture as code-loaded validation data: expand it deliberately, and use explicit accepted subject/scene aliases when Gemini review needs semantically broader matching than one literal phrase. — refs: `tests/fixtures/ltx/prompt_scenarios.json`, `scripts/ltx_showcase_generate.py`, `tests/test_ltx_showcase_generate.py`
- (2026-03-08) [AUDIO-LATENT-CONTRACT] The current artifactized LTX bundle may not include `audio_vae/config.json`, so audio latent mel bins must be derived from the transformer contract, not guessed from missing audio-VAE defaults. For the current `LTX-2.3` fast path, `audio_in_channels=128` and `audio_encoder.latent_channels=8` imply `audio_latent_mel_bins=16`. If that inference is wrong, the real runtime fails in the transformer audio preprocessor with a shape mismatch even when the rest of the backend is healthy. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/config.py`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/runtime_helpers.py`, `tests/test_runtime_units.py`, `references/official/LTX-2/packages/ltx-core/README.md`
- (2026-03-08) [AUDIO-DECODER-WEIGHTS] The audio decoder loader must receive raw checkpoint audio weights exactly once. Pre-sanitizing and re-prefixing `audio_vae.*` weights before `_load_runtime_audio_decoder()` causes a second sanitation/remap pass and breaks the decoder conv layout on real artifact-backed runs. The correct seam is: load the unified checkpoint weights once, pass the raw prefixed mapping into the decoder loader, and let the runtime loader own the sanitation. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/runtime_helpers.py`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/video_stack.py`, `tests/test_runtime_units.py`
- (2026-03-08) [DONOR-FREE-DISTILLED] The promoted distilled LTX generation engine is now donor-free for `mlx_video` runtime imports. The owned engine now covers latent upsampling, VAE encoder/decoder-side helpers, audio autoencoder/BWE stack, rotary embeddings, AdaLN, transformer preprocessors, attention, transformer blocks, and the top-level `LTXModel` bridge inside the LTX family package. The next donor-removal wave is the Gemma prompt path through `mlx-vlm` / `mlx-lm`, guided by `docs/research/17-multimodal-inference-engine.md`. — refs: `docs/research/16-owned-substrate-migration-plan.md`, `docs/families/ltx-compatibility-checklist.md`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/transformer_model.py`
- (2026-03-08) [OWNED-GEMMA-SUBSTRATE] The Gemma prompt path is now donor-free at runtime too. `packages/core/runtime-mlx-models/` is the first shared core MLX model substrate and now owns the minimal Gemma 3 text runtime, KV-cache primitives, RoPE helpers, and attention-mask helpers used by LTX prompt encode. The next question is no longer “remove `mlx-vlm` / `mlx-lm`”; it is “which pieces of tokenizer / processor / multimodal model execution truly belong in shared core next?” — refs: `packages/core/runtime-mlx-models/`, `packages/families/ltx/src/mlxr/families/ltx/_prompt_encoding_backend/runtime.py`, `docs/research/16-owned-substrate-migration-plan.md`, `docs/research/17-multimodal-inference-engine.md`
- (2026-03-08) [TOKENIZER-POLICY] The shared core MLX model substrate now owns a thin repo-owned tokenizer wrapper too. Current policy is deliberate: core owns local tokenizer loading, tokenization into typed arrays, and basic detokenization; it does not yet widen into a full chat-template / streaming / processor surface. `transformers` remains an explicit runtime dependency for tokenizer loading in this first slice, while processor policy stays a separate decision. — refs: `packages/core/runtime-mlx-models/src/mlxr/core/mlx_models/tokenization.py`, `packages/families/ltx/src/mlxr/families/ltx/_prompt_encoding_backend/encoder.py`, `docs/research/17-multimodal-inference-engine.md`, `docs/families/ltx-compatibility-checklist.md`
- (2026-03-08) [LTX-SMOKE-GUARDRAILS] `scripts/ltx_debug_smoke.py` is now explicitly transitional engine-diagnostic tooling, not the long-term product surface. It has hard safety guardrails for MLX memory and wall-clock limits, writes abort receipts into `run_manifest.json`, and should only be used for family-engine diagnosis until equivalent trace/guardrail controls are absorbed into the canonical `mlxr` CLI. Treat smoke receipts as engine-health proof unless Gemini/manual review separately clears fidelity. — refs: `scripts/ltx_debug_smoke.py`, `docs/research/10-ltx-fidelity-debug-playbook.md`, `scripts/README.md`
- (2026-03-09) [PRODUCT-SURFACE-DISCIPLINE] Family-specific parity or validation ideas must not leak into the shared workflow/CLI product surface as ad hoc flags. If a control is only useful for one current family or one debugging moment, keep it family-local or diagnostic-only until it becomes a clean typed cross-family concept. `AGENTS.md` is the doctrine source for this rule; keep `MEMORY.md` as the reminder that this repo already tripped over that seam once. — refs: `AGENTS.md`, `docs/workflow-orchestration-design.md`
- (2026-03-07) [WORKFLOW-LAYER] `MLXR` now has a shared core workflow-planning layer between scheduler, family adapters, and host adapters. Today it owns intent normalization, family-dispatched planning, and conversion into the primitive job API; it does not yet own full stage-graph execution. Families still own truthful workflow templates and stage implementations, and hosts stay thin. — refs: `docs/workflow-orchestration-design.md`, `AGENTS.md`, `docs/agent-native-development.md`
- (2026-03-07) [FAMILY-BRINGUP] The default bring-up pack is now `docs/family-bringup/` plus `.agents/skills/family-bringup/`. New-family work should start from the smallest truthful slice, record source/artifact/workflow/capability contracts explicitly, and complete a fresh-eyes review before commit. — refs: `docs/family-bringup/README.md`, `docs/family-bringup/checklist.md`, `docs/family-bringup/template.md`, `.agents/skills/family-bringup/SKILL.md`
- (2026-03-09) [FAMILY-DEBUGGING-LADDER] When a new family "runs but looks wrong", use the layered bring-up debug method in `docs/family-bringup/debugging-decision-tree.md`: lock source truth, lock artifact truth, prove tiny component parity, then isolate conditioning versus image/output path with swap tests before promoting the canonical CLI/runtime result. The `Z-Image-Turbo` bring-up specifically showed that reference prompt embeddings can be the decisive isolation seam; coherent output under reference embeddings means the image stack may already be sound. — refs: `docs/family-bringup/debugging-decision-tree.md`, `packages/families/z-image/src/mlxr/families/z_image/_prompt_encoding_backend.py`, `packages/families/z-image/src/mlxr/families/z_image/_generation_backend.py`
- (2026-03-09) [Z-IMAGE-BASE-TRACE] The `z_image` workflow path now preserves family-local `extensions.z_image` values, the MLX backend supports `cfg_normalization` and `cfg_truncation` without widening the shared product surface, and env `MLXR_Z_IMAGE_DEBUG_TRACE=1` records family trace metadata inside the generate-stage metrics event. The first balanced base receipt is `tmp/manual-runs/20260309T195155Z-zimage-base-balanced-rung/` at `768x768 / 28 steps / guidance=4.0 / cfg_normalization=True`, with generate duration about `169.9s`, peak active memory about `29.3 GB`, and a coherent lighthouse-keeper image. A larger `1024x1024 / 36-step` base attempt exceeded a quick dev-loop budget on this machine and should be treated as benchmark work, not a default smoke rung. — refs: `packages/families/z-image/src/mlxr/families/z_image/workflows.py`, `packages/families/z-image/src/mlxr/families/z_image/family_options.py`, `packages/families/z-image/src/mlxr/families/z_image/_generation_backend.py`, `tmp/manual-runs/20260309T195155Z-zimage-base-balanced-rung/run_manifest.json`, `tmp/zimage-base-runtime/jobs/job_b82797e5ff0b4cf7aa4bd0234d885c53/events.jsonl`
- (2026-03-09) [LTX-KEYED-IMAGE-BRIDGE] The canonical CLI/runtime path now supports multiple keyed image references on the current distilled `video.condition.image` row via repeatable `--keyframe-image PATH FRAME_IDX [STRENGTH]`. Treat that as a truthful current bridge for first/last-frame-style I2V, not as official `KeyframeInterpolationPipeline` semantics. The first manual receipt is `tmp/manual-runs/20260309T2324Z-zimage-ltx-keyframe-samurai-to-fire.mp4`. The LTX family-local advanced extension seam now validates `workflow_variant`, `control_variant`, and `conditioning_attention_strength`, but `control_variant` remains reserved for future `video.condition.video` work and must fail closed outside that task. — refs: `packages/clients/runtime-cli/src/mlxr/clients/cli/cli.py`, `packages/families/ltx/src/mlxr/families/ltx/workflows.py`, `packages/families/ltx/src/mlxr/families/ltx/family_options.py`, `tests/test_runtime_workflows.py`, `tmp/manual-runs/20260309T2324Z-zimage-ltx-keyframe-samurai-to-fire.mp4`
- (2026-03-10) [MODEL-INSTALL-SURFACE] Product-default model acquisition should reuse the normal Hugging Face cache as the source layer when possible, then convert into a separate runtime-managed portable artifact store, then optionally build a separate machine-local execution cache. Temporary `tmp/hf-...-live/` mirrors are valid family-bring-up tooling, not the long-term product contract. — refs: `docs/model-acquisition-and-cache.md`, `docs/provider-and-provenance-model.md`
- (2026-03-10) [HEAVY-JOB-SERIALIZATION] The safe local default for Apple Silicon media generation is queueing allowed, but heavyweight image/video execution serialized per worker; do not rely on co-resident loading of multiple large model stacks as a default behavior. The architecture docs already point this way, and future scheduler changes should preserve that conservative default unless benchmarked evidence justifies broader concurrency. — refs: `docs/model-acquisition-and-cache.md`, `docs/technical-design.md`, `docs/adr/0004-worker-process-topology.md`, `packages/core/runtime-server/src/mlxr/core/server/worker.py`
- (2026-03-10) [OWNED-MLX-ONLY] `FLUX` and `Qwen-Image` execution must follow the same standard as `z_image`: owned `MLXR` implementation on top of official `MLX` and shared core libraries only. Do not use third-party runtime bridge libraries as dependencies, and do not use their codebases as implementation templates; use official upstream model contracts and our own design instead. — refs: `AGENTS.md`, `packages/families/flux2/src/mlxr/families/flux2/generation.py`, `packages/families/qwen-image/src/mlxr/families/qwen_image/generation.py`
- (2026-03-10) [SOURCE-BUNDLE-NAMES] Upstream Hugging Face bundles may legitimately contain filenames like `diffusion_pytorch_model.safetensors` even when the runtime path is fully owned MLX. Treat those filenames as source-bundle contract only; the execution question is whether `MLXR` loads the tensors into its own MLX modules, not what the upstream file happened to be named. — refs: `docs/model-acquisition-and-cache.md`, `packages/families/flux2/src/mlxr/families/flux2/_generation_backend/loading.py`
- (2026-03-10) [FLUX-KLEIN-RUNTIME] The owned FLUX.2 `klein` runtime is now real for `flux.2-klein-4b` and `flux.2-klein-9b` prompt-only `image.generate` plus single-reference `image.edit` through the canonical `mlxr` path. Distilled `klein` rows must stay fail-closed to `num_inference_steps=4` and `guidance_scale=1.0`. — refs: `packages/families/flux2/README.md`, `docs/families/flux2-capability-matrix.md`
- (2026-03-12) [FLUX-KV-SUBSTRATE] The owned FLUX.2 `flux.2-klein-9b-kv` row now has a dedicated family-local MLX KV-cache substrate. Prompt-only `image.generate` stays on the normal distilled path, while reference-conditioned `image.edit` now extracts reference-token K/V on step `0` and reuses them on later denoise steps. A first canonical `mlxr` single-ref smoke proves the end-to-end KV path and stage metrics, but the first same-machine timing receipt on a tiny safe rung is slower than plain `9b` (`11.21s` plain single-ref median versus `14.50s` KV; `11.25s` plain multi-ref versus `15.49s` KV), so the row is explicitly not promotion-ready yet. — refs: `packages/families/flux2/src/mlxr/families/flux2/_generation_backend/transformer.py`, `packages/families/flux2/src/mlxr/families/flux2/_generation_backend/runtime.py`, `tmp/flux2-kv-canonical-smoke-20260312T2045Z/runtime-home/jobs/job_5fd927e67fa843e3a16bf03336c9e2ce/events.jsonl`, `tmp/flux2-kv-canonical-smoke-20260312T2045Z/benchmark-summary.json`, `docs/families/flux2-capability-matrix.md`
- (2026-03-10) [FLUX-MULTIREF-LIMIT] The current owned FLUX.2 multi-reference edit path is implemented but not quality-promoted. Real `4b` and `9b` receipts currently collapse too strongly toward the later reference image instead of blending the requested structure and style sources, and Gemini reviews call both rows `mismatch`. — refs: `docs/families/flux2-capability-matrix.md`, `tmp/manual-runs/20260310T1428Z-flux2-klein9b-cli-edit-multiref-review/gemini-image-review.json`, `tmp/manual-runs/20260310T1430Z-flux2-klein4b-cli-edit-multiref-review/gemini-image-review.json`
- (2026-03-10) [FAMILY-SHAPE-FLUX] The FLUX family should follow the same maintainability pattern as the LTX generation backend: small focused modules under `_generation_backend/`, with tests split under `tests/unit/generation/` and `tests/unit/prompt/` instead of growing another monolithic backend file plus flat test pile. — refs: `packages/families/flux2/src/mlxr/families/flux2/_generation_backend/`, `packages/families/flux2/tests/unit/generation/`, `packages/families/flux2/tests/unit/prompt/`
- (2026-03-10) [FLUX-BASE-CFG] Owned FLUX base `klein` rows now use the official unconditional-plus-prompt CFG branch rather than the distilled guidance-embed path. Keep `flux.2-dev` fail-closed until its separate prompt stack exists; do not quietly reuse the Qwen `klein` path for `dev`. — refs: `packages/families/flux2/src/mlxr/families/flux2/_generation_backend/runtime.py`, `references/official/flux2/scripts/cli.py`, `references/official/flux2/src/flux2/sampling.py`
- (2026-03-10) [TEST-HARNESS-PACKAGE-IMPORTS] `scripts/run_unittests.py` now loads package-local tests under a synthetic dotted `__mlxr_tests__.*` package tree so nested family tests can use sibling helpers like `from ._fixtures import ...` without flattening back into monolithic files. If package-local test imports start breaking after a harness change, check this loader first. — refs: `scripts/run_unittests.py`, `docs/test-layout.md`
- (2026-03-10) [FLUX-BASE-4B-RECEIPT] The first truthful owned FLUX base receipt is now `flux.2-klein-base-4b` `image.generate` through the canonical `mlxr` CLI/runtime path. The promoted receipt is `tmp/manual-runs/20260310T162836Z-flux2-klein-base-4b-cli-generate.png` with strict Gemini match review under `tmp/manual-runs/20260310T162836Z-flux2-klein-base-4b-cli-generate-review/`; the run used `512x512 / 50 steps / guidance=4.0`, took about `84.1s` in `generate`, and peaked around `20.5 GB` active memory. — refs: `tmp/manual-runs/20260310T162836Z-flux2-klein-base-4b-cli-generate.png`, `tmp/manual-runs/20260310T162836Z-flux2-klein-base-4b-cli-generate-review/gemini-image-review.json`, `/tmp/mlxr-flux2-base-runtime/jobs/job_9d80c13d75e74b28b476446cd2154636/events.jsonl`
- (2026-03-10) [FLUX-BASE-EDIT-SCOPE] `flux.2-klein-base-4b` single-reference editing is real but not fully promoted yet. A preservation-heavy edit receipt under `tmp/manual-runs/20260310T1646Z-flux2-klein-base-4b-cli-edit-single.png` changed the coat successfully but drifted the compass into a clock and dropped the glove, so treat base-row editing as prompt-sensitive until simpler real receipts and clean semantic reviews are pinned. — refs: `tmp/manual-runs/20260310T1646Z-flux2-klein-base-4b-cli-edit-single.png`, `tmp/manual-runs/20260310T1646Z-flux2-klein-base-4b-cli-edit-single-review/`, `docs/families/flux2-capability-matrix.md`
- (2026-03-10) [FLUX-BASE-9B-RECEIPT] The heavyweight owned base row is now real too: `flux.2-klein-base-9b` `image.generate` completed through the canonical `mlxr` CLI/runtime path as `tmp/manual-runs/20260310T1731Z-flux2-klein-base-9b-cli-generate.png` with strict Gemini match review under `tmp/manual-runs/20260310T1731Z-flux2-klein-base-9b-cli-generate-review/`. The first receipt used `512x512 / 50 steps / guidance=4.0`, took about `217.4s` in `generate`, and peaked around `37.98 GB` active memory. — refs: `tmp/manual-runs/20260310T1731Z-flux2-klein-base-9b-cli-generate.png`, `tmp/manual-runs/20260310T1731Z-flux2-klein-base-9b-cli-generate-review/gemini-image-review.json`, `/tmp/mlxr-flux2-base-runtime/jobs/job_3c159aea1d7040bd8511ec58fae248ef/events.jsonl`
- (2026-03-10) [QWEN-2512-PROMPT-ENCODE] The first owned `Qwen-Image-2512` execution seam is now real at the prompt-conditioning layer. `mlxr.families.qwen_image` has a family-local prompt encoder on top of a new owned `Qwen2.5-VL` text-only MLX substrate, and it follows the official fixed scaffold plus `34`-token prefix drop instead of using the generic tokenizer chat template. A real local smoke against `tmp/hf-qwen-image-2512-live/text_encoder` produced prompt embeddings with shape `(1, 17, 3584)`. — refs: `packages/core/runtime-mlx-models/src/mlxr/core/mlx_models/qwen2_text.py`, `packages/families/qwen-image/src/mlxr/families/qwen_image/_prompt_encoding_backend.py`, `packages/families/qwen-image/tests/unit/test_prompt_encoding.py`
- (2026-03-10) [QWEN-2512-BACKEND-SPINE] `qwen_image` now uses an explicit `prompt_encode -> generate -> encode_output` stage order like `z_image`, and the package has a first owned backend spine under `packages/families/qwen-image/src/mlxr/families/qwen_image/_generation_backend/` for config parsing, flow-match scheduler behavior, latent pack/unpack, and fail-closed runtime scaffolding. Full image generation is still not promoted, but the backend shape is now modular rather than a flat placeholder. — refs: `packages/families/qwen-image/src/mlxr/families/qwen_image/adapter.py`, `packages/families/qwen-image/src/mlxr/families/qwen_image/_generation_backend/`, `packages/families/qwen-image/tests/unit/generation/`
- (2026-03-10) [QWEN-2512-FIRST-SMOKE] The canonical `mlxr generate` path for `qwen-image-2512` now produces a coherent first smoke receipt at `tmp/manual-runs/qwen2512-cli-smoke-sigmafix.png`. The biggest bring-up blocker was a scheduler contract bug: the owned Qwen flow-match Euler step was incorrectly updating latents with `1000`-scaled timesteps instead of sigma deltas, which blew up the denoise trajectory even when prompt encode and transformer parity looked reasonable. The prompt and image stacks still have further parity work, but the repo truth is no longer “Qwen generation is only noise.” — refs: `packages/families/qwen-image/src/mlxr/families/qwen_image/_generation_backend/runtime.py`, `packages/families/qwen-image/src/mlxr/families/qwen_image/_generation_backend/scheduler.py`, `packages/families/qwen-image/tests/unit/generation/test_scheduler.py`, `tmp/manual-runs/qwen2512-cli-smoke-sigmafix.png`, `/tmp/mlxr-qwen-cli-runtime/jobs/job_0542ae6b24604d538c29caea054a105f/events.jsonl`
- (2026-03-10) [QWEN-LIGHTNING-FIRST-RECEIPT] The first owned Qwen Lightning receipt is now real through the canonical `mlxr` path: `tmp/manual-runs/qwen2512-lightning-cli-512.png` used the generic `--lora` surface with the `lightx2v/Qwen-Image-2512-Lightning` `4`-step LoRA plus family-local `qwen_image.scheduler_preset=lightning`. The streamed local LoRA import unblocked the path, and the resulting `generate` stage took about `97.2s` at `512x512 / 4 steps / guidance=1.0`, versus about `266.4s` for the standard non-Lightning `512x512` `8`-step run. — refs: `packages/families/qwen-image/src/mlxr/families/qwen_image/_generation_backend/lora.py`, `packages/clients/runtime-cli/src/mlxr/clients/cli/cli.py`, `packages/core/runtime-server/src/mlxr/core/server/app.py`, `tmp/manual-runs/qwen2512-lightning-cli-512.png`, `/tmp/mlxr-qwen-lightning-runtime/jobs/job_8b362614d6004f119644784731f91a7e/events.jsonl`
- (2026-03-10) [QWEN-WULI-FIRST-RECEIPT] `Wuli-Art/Qwen-Image-2512-Turbo-LoRA` `V3.0` now works on the owned Qwen backend too, but it needed a real compatibility pass: normalize `diffusion_model.*` prefixes, map `lora_A` / `lora_B` onto down/up, and alias modulation targets like `img_mod.1` / `txt_mod.1` onto the owned transformer layer names. The first truthful receipt is `tmp/manual-runs/qwen2512-wuli-v3-4step-wide-512x288.png`, completed through the real runtime job path with `qwen_image.scheduler_preset=turbo_wuli`; its `generate` stage took about `85.5s` at `512x288 / 4 steps / guidance=1.0`. The wider `704x384` rung hit a Metal GPU timeout on this machine, so keep the current boundary honest. — refs: `packages/families/qwen-image/src/mlxr/families/qwen_image/_generation_backend/lora.py`, `packages/families/qwen-image/tests/unit/generation/test_lora.py`, `tmp/manual-runs/qwen2512-wuli-v3-4step-wide-512x288.png`, `/tmp/mlxr-qwen-lightning-runtime/jobs/job_ad9e77722c084977ad94050dfb36a5e1/events.jsonl`
- (2026-03-11) [QWEN-EDIT-FIRST-OWNED-PATH] The owned `Qwen-Image-Edit-2511` backend is now real end to end: prompt encoding uses the official processor path plus owned Qwen2.5-VL vision/text substrate, the transformer supports edit-row `zero_cond_t` modulation, and the VAE path now encodes as well as decodes. The first direct base edit receipt `tmp/manual-runs/qwen-edit-direct-1step.png` stays useful only as a probe rung; the real promotion bar is the official recipe. That official base receipt now exists at `tmp/showcase-runs/qwen-official-eval-20260311T221605Z/kenji_edit_base40_1344x768.png`, completed through the canonical `mlxr` path at `1344x768 / 40 steps / guidance=4.0` with `generate` about `1574.7s`. — refs: `packages/families/qwen-image/src/mlxr/families/qwen_image/_prompt_encoding_backend.py`, `packages/families/qwen-image/src/mlxr/families/qwen_image/_generation_backend/runtime.py`, `tmp/manual-runs/qwen-edit-direct-1step.png`, `tmp/showcase-runs/qwen-official-eval-20260311T221605Z/summary.csv`
- (2026-03-11) [QWEN-EDIT-LIGHTNING-PRACTICAL] Lightning remains the practical fast Qwen edit lane on this machine, but it is no longer the only truthful edit path. The official-recipe Lightning comparison receipt now exists too at `tmp/showcase-runs/qwen-official-eval-20260311T221605Z/kenji_edit_lightning4_1344x768.png`, with `generate` about `96.9s`. Use the pair of official receipts to tell the honest product story: base for maximum fidelity lane, Lightning for practical speed lane. — refs: `tmp/showcase-runs/qwen-official-eval-20260311T221605Z/kenji_edit_lightning4_1344x768.png`, `tmp/showcase-runs/qwen-official-eval-20260311T221605Z/summary.csv`
- (2026-03-11) [QWEN-EDIT-MULTIREF] The practical current `Qwen-Image-Edit-2511` row is now proven multi-image too. The first canonical multi-image Lightning receipt is `tmp/manual-runs/qwen-edit-cli-lightning-4step-multiref.png`, completed as job `job_8357e3b17a614d8089ce32752e7faa17` with `generate` about `124.6s`, prompt encode about `1.9s`, peak active memory about `64.04 GB`, and a semantically plausible fused samurai result instead of collapse. — refs: `tmp/manual-runs/qwen-edit-cli-lightning-4step-multiref.png`, `/tmp/mlxr-qwen-edit-runtime/jobs/job_8357e3b17a614d8089ce32752e7faa17/events.jsonl`
- (2026-03-10) [LORA-TRANSPORT] Generic `--lora PATH [STRENGTH]` is the right shared CLI surface, but large adapter files should not stay on the JSON-base64 import path forever. Reusable large LoRAs should trend toward installed/registered adapter identities backed by pinned Hugging Face snapshots or trusted local bundles, and ad hoc local imports should use streamed local file transport rather than base64 payloads. — refs: `docs/model-acquisition-and-cache.md`, `packages/clients/runtime-cli/src/mlxr/clients/cli/cli.py`, `packages/core/runtime-server/src/mlxr/core/server/app.py`
- (2026-03-10) [TEXT-INFERENCE-SURFACE] When `MLXR` grows text-generation product surfaces, the core runtime should own one canonical text/conversation inference contract and expose OpenAI-style responses/chat and Anthropic-style messages as thin host-compatible adapters. Do not let provider-compatible HTTP shapes become the de facto core runtime model or leak model-family prompt semantics into the shared surface. — refs: `AGENTS.md`, `docs/technical-design.md`, `docs/workflow-orchestration-design.md`
- (2026-03-11) [BASE-FIRST-EVALUATION] For image families, a minimal smoke or probe rung must never be promoted into a base-model quality conclusion. Evaluate base models on the official upstream recipe first, then establish a practical local base rung, then compare adapters like Lightning or turbo on the same prompt, seed, task, and resolution. Qwen edit specifically should not be described as “base broken” just because the first `256x256 / 1-step` probe returned a black frame. — refs: `docs/family-bringup/evaluation-framework.md`, `packages/families/qwen-image/README.md`, `docs/families/qwen-image-family-notes.md`
- (2026-03-11) [QWEN-OFFICIAL-HEAVY-RUNG] The heavyweight official-recipe Qwen generation lane is now genuinely healthy through the canonical `mlxr` CLI/runtime path. Clean `1664x928` receipts now exist for base `50-step`, LightX `8-step`, and Wuli `4-step`: `tmp/showcase-runs/qwen-official-eval-20260311T191251Z/floating_city_base50_1664x928.png` at about `1253.9s`, `tmp/showcase-runs/qwen-official-eval-20260311T191251Z/floating_city_lightx8_1664x928.png` at about `151.0s`, and `tmp/showcase-runs/qwen-official-eval-20260311T190800Z/floating_city_wuli4_1664x928.png` at about `108.5s`. — refs: `scripts/qwen_official_eval.py`, `tmp/showcase-runs/qwen-official-eval-20260311T190800Z/summary.csv`, `tmp/showcase-runs/qwen-official-eval-20260311T191251Z/summary.csv`
- (2026-03-11) [QWEN-DECODE-ORACLE] The fix for the shared Qwen full-resolution corruption was not prompt tuning or a LoRA change; it was large-image VAE parity. `scripts/qwen_decode_oracle.py` now saves one same-latent comparison bundle, and the owned Qwen autoencoder now uses upstream-style tiled encode/decode behavior. The canonical first proof is `tmp/manual-runs/qwen-decode-oracle-20260311T190307Z/`, which contains `owned_decode.png`, `diffusers_decode.png`, and `diffusers_tiled_decode.png` from the same saved latents. If future high-resolution Qwen output looks wrong again, start there before guessing about schedulers or prompt scaffolds. — refs: `scripts/qwen_decode_oracle.py`, `packages/families/qwen-image/src/mlxr/families/qwen_image/_generation_backend/autoencoder.py`, `tmp/manual-runs/qwen-decode-oracle-20260311T190307Z/manifest.json`
- (2026-03-11) [LTX-DEV-ARTIFACT-CONTRACT] The current owned `LTX` `dev` row is no longer forced through the fast bundle shape. The `ltx-2.3-22b-dev.safetensors` artifact path now converts with only `checkpoint + text_encoder` required for the implemented `one_stage` lane, while optional future-facing components like the x2 upsampler and `ltx-2.3-22b-distilled-lora-384.safetensors` can still be carried on the artifact without claiming those rows are active. — refs: `packages/families/ltx/src/mlxr/families/ltx/_adapter/artifacts.py`, `packages/families/ltx/src/mlxr/families/ltx/_adapter/capability.py`, `docs/working/ltx-closure-plan.md`
- (2026-03-11) [LTX-TWO-STAGE-SUBSTRATE] The owned `LTX` `dev` path now has a real internal `two_stage` substrate: official distilled LoRA loading is explicit, the transformer loader applies LoRA deltas fail-closed, and `ltx.workflow_variant=two_stage` is only exposed on `dev` artifacts that actually carry both the x2 upsampler and `ltx-2.3-22b-distilled-lora-384.safetensors`. Treat that row as implemented but unvalidated until real receipts exist. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/lora.py`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/two_stage.py`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/runtime_helpers.py`, `tests/test_phase_a_runtime.py`, `tests/test_runtime_workflows.py`
- (2026-03-12) [LTX-RETAKE-SUBSTRATE] `video.retake` is now real through the canonical `mlxr` CLI/workflow/server path on the owned `LTX` runtime. The shared surfaces carry one bound source-video reference plus `window_start_seconds`, `window_end_seconds`, and regenerate toggles; the backend encodes source video/audio latents, builds temporal retake masks, preserves content outside the selected window, and reuses the existing distilled denoise/decode substrate. Treat the row as implemented but unvalidated until real receipts exist. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/retake.py`, `packages/families/ltx/src/mlxr/families/ltx/_adapter/stages.py`, `packages/families/ltx/src/mlxr/families/ltx/workflows.py`, `packages/core/runtime-server/src/mlxr/core/server/jobs.py`, `packages/core/runtime-server/src/mlxr/core/server/worker.py`, `tests/test_runtime_workflows.py`, `tests/test_runtime_units.py`
- (2026-03-12) [LTX-HQ-SUBSTRATE] `two_stage_hq` is now a real owned `LTX` `dev` runtime row. It is only exposed on `dev` artifacts that actually carry both the x2 upsampler and the official distilled LoRA, stage-specific distilled-LoRA strengths live under `extensions.ltx`, and the backend runs an owned `res_2s` loop instead of silently replacing standard two-stage. Treat the row as implemented but unvalidated until a real HQ receipt exists. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/two_stage_hq.py`, `packages/families/ltx/src/mlxr/families/ltx/family_options.py`, `packages/families/ltx/src/mlxr/families/ltx/_adapter/capability.py`, `tests/test_runtime_workflows.py`
- (2026-03-12) [LTX-DEV-MEANINGFUL-RUNS] The first meaningful `6s / 768x448 / 145f / 24fps` `dev` receipts now exist for both standard `two_stage` and `two_stage_hq`. Standard two-stage completed as `tmp/manual-runs/20260312T1533Z-ltx-two-stage-meaningful-rerun/ltx_dev_two_stage_768x448_145f.mp4` with `generate` about `285.7s`, peak active memory about `77.3 GB`, and a Gemini `mismatch` verdict. HQ completed as `tmp/manual-runs/20260312T1549Z-ltx-dev-two-stage-hq-6s/ltx_dev_two_stage_hq_768x448_145f.mp4` with `generate` about `358.0s` and another Gemini `mismatch` verdict. Treat both rows as runtime-stable at the first meaningful rung, but still not quality-promoted. — refs: `/tmp/mlxr-ltx-matrix-runtime/jobs/job_984b16b81c8447668f9e223ced984cf7/events.jsonl`, `/tmp/mlxr-ltx-matrix-runtime/jobs/job_680c139e866243aebbc548f2bd8b89eb/events.jsonl`, `tmp/manual-runs/20260312T1533Z-ltx-two-stage-meaningful-rerun/gemini-review/gemini-conditioned-review.json`, `tmp/manual-runs/20260312T1549Z-ltx-dev-two-stage-hq-6s/gemini-review/gemini-conditioned-review.json`
- (2026-03-12) [LTX-ONE-STAGE-MEANINGFUL-RUN] The first meaningful `6s / 768x448 / 145f / 24fps` `one_stage` receipt is now real too: `tmp/manual-runs/20260312T1600Z-ltx-dev-one-stage-6s/ltx_dev_one_stage_768x448_145f.mp4` completed with `generate` about `684.7s`, peak active memory about `43.57 GB`, and a Gemini `partial` verdict. Treat one-stage as runtime-stable at this rung but substantially weaker than the two-stage family; it still fits the official “educational / prototyping” role better than a production-quality claim. — refs: `/tmp/mlxr-ltx-matrix-runtime/jobs/job_17aa4970874a4f67aa6089d1f79a0fae/events.jsonl`, `tmp/manual-runs/20260312T1600Z-ltx-dev-one-stage-6s/gemini-review/gemini-conditioned-review.json`
- (2026-03-12) [LTX-RETAKE-MEANINGFUL-RUN] The first meaningful `6s / 768x448 / 145f / 24fps` `video.retake` receipt is now real on the fast path: `tmp/manual-runs/20260312T1612Z-ltx-fast-retake-6s/ltx_fast_retake_768x448_145f.mp4` completed with `generate` about `222.6s`, peak active memory about `49.32 GB`, and a Gemini `mismatch` verdict. The current retake lane is runtime-stable at this rung, but it preserved the running-dog scene more than it inserted the requested new “jump over a bench” beat, so retake semantics are still not promotion-strong. — refs: `/tmp/mlxr-ltx-matrix-runtime/jobs/job_66c87fe1d4314f3484f25cf5a32bfbba/events.jsonl`, `tmp/manual-runs/20260312T1612Z-ltx-fast-retake-6s/gemini-review/gemini-conditioned-review.json`
- (2026-03-12) [LTX-MOTION-TRACK-SURFACE] `Lightricks/LTX-2.3-22b-IC-LoRA-Motion-Track-Control` should be treated as another control asset on the existing `video.condition.video` row, not as a new task. The current product surface now accepts `extensions.ltx.control_variant=motion_track_control`, but it is still only a label until the motion-track LoRA itself has real validation receipts. — refs: `packages/families/ltx/src/mlxr/families/ltx/family_options.py`, `packages/families/ltx/src/mlxr/families/ltx/_adapter/capability.py`
- (2026-03-12) [LTX-IC-LORA-MEANINGFUL-RUNS] The fast `video.condition.video` row now has meaningful `6s / 768x448 / 145f / 24fps` receipts. `union_ic_lora` completed as `tmp/manual-runs/20260312T1619Z-ltx-fast-union-ic-lora-6s/ltx_fast_union_ic_lora_768x448_145f.mp4` with `generate` about `322.8s`, peak active memory about `64.70 GB`, and a Gemini `match` verdict for the fox-running scene. `motion_track_control` completed as `tmp/manual-runs/20260312T1626Z-ltx-fast-motion-track-6s/ltx_fast_motion_track_768x448_145f.mp4` with `generate` about `325.7s`, peak active memory about `64.69 GB`, and a Gemini `partial` verdict. Treat union-control as the strongest currently validated fast control slice, and motion-track as real but weaker. — refs: `/tmp/mlxr-ltx-matrix-runtime/jobs/job_6a32601fc12c4978861d8146507e0be2/events.jsonl`, `/tmp/mlxr-ltx-matrix-runtime/jobs/job_3bb72a017b47421980cf38781225dc89/events.jsonl`, `tmp/manual-runs/20260312T1619Z-ltx-fast-union-ic-lora-6s/gemini-review/gemini-conditioned-review.json`, `tmp/manual-runs/20260312T1626Z-ltx-fast-motion-track-6s/gemini-review/gemini-conditioned-review.json`
- (2026-03-12) [LTX-INTERPOLATE-MEANINGFUL-RUN] The first meaningful `6s / 768x448 / 145f / 24fps` interpolation receipt is now real too: `tmp/manual-runs/20260312T1638Z-ltx-interpolate-fox-6s/ltx_interpolate_fox_768x448_145f.mp4` completed with `generate` about `279.5s`, peak active memory about `77.32 GB`, and a Gemini `mismatch` verdict. The row is runtime-stable, but the first fox-keyframe run ignored the start and end frames badly enough that interpolation must stay unpromoted until keyframe adherence improves. — refs: `/tmp/mlxr-ltx-matrix-runtime/jobs/job_0edd9054f3fd43dc82a0035997090178/events.jsonl`, `tmp/manual-runs/20260312T1638Z-ltx-interpolate-fox-6s/gemini-review/gemini-conditioned-review.json`
- (2026-03-12) [LTX-INTERPOLATION-SUBSTRATE] `video.interpolate` is now real on the owned `LTX` `dev` two-stage path. The key semantic difference from the older keyed-image bridge is that interpolation treats every keyed image, including frame zero, as a guiding keyframe instead of replacing the first latent; the workflow defaults `video.interpolate` to `ltx.workflow_variant=two_stage`, requires at least two keyed images at distinct frame indices, and keeps the older multi-image `video.condition.image` bridge honest as a separate row. Treat interpolation as implemented but unvalidated until a real receipt exists. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/interpolate.py`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/runtime_helpers.py`, `packages/families/ltx/src/mlxr/families/ltx/workflows.py`, `packages/core/runtime-server/src/mlxr/core/server/jobs.py`, `tests/test_runtime_workflows.py`, `packages/families/ltx/tests/unit/test_workflows.py`
