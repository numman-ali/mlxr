# MEMORY.md

This file is the durable cross-session memory for `MLXR`.

Use it to preserve stable repo-operating learnings so fresh Codex sessions do not have to rediscover them.

## How To Use This File

### Tier 1 — Must Read

- Read this section at the start of every session, immediately after `AGENTS.md`.
- Keep it short and durable.
- Promote only facts that should shape most future sessions.

### Tier 2 — Lookup Log

- Do not read this end to end by default.
- Search it when working on a related area.
- Append durable learnings here at the end of a session.

## Memory Update Rule

- Add durable learnings to Tier 2.
- Promote only cross-session critical items to Tier 1.
- Keep Tier 1 focused on durable repo facts and sharp edges, not doctrine already stated in `AGENTS.md`.
- If a rule belongs in repo operating doctrine, put it in `AGENTS.md` instead of repeating it here.
- Archive obsolete items instead of silently deleting them.
- Do not put transient task notes or diary-style entries here.

## Tier 1 — Must Read

- The default local quality gate is `uv run python scripts/dev.py verify`.
- `pre-commit` is a fast hygiene mirror, not the main acceptance gate.
- If runtime behavior changes, inspect `$MLX_RUNTIME_HOME/logs/control-plane.log` as part of validation.
- `verify` and `pre-commit` run `scripts/check_type_escapes.py`; avoid `typing.cast` and explicit `Any` in the scanned runtime and test surfaces.
- Placeholder package folders stay outside the `uv` workspace until they gain a real `pyproject.toml`.
- Python imports now use the shared `mlxr.*` namespace; run type checks through `scripts/run_mypy.py` because namespace-package repos need package-targeted mypy invocation rather than a single flat file crawl.

## Tier 2 — Lookup Log

- (2026-03-06) [HARNESS] The repo uses explicit `uv` workspace membership and `tool.uv.sources`; root `uv sync`, `uv lock`, and `uv run` should work without extra flags. — refs: `pyproject.toml`, `uv.lock`
- (2026-03-06) [QUALITY] The current local quality stack is `ruff`, `mypy --strict`, `unittest`, and `uv build --all-packages`. — refs: `pyproject.toml`, `scripts/dev.py`
- (2026-03-06) [LOGS] The control plane logs to `$MLX_RUNTIME_HOME/logs/control-plane.log`, and runtime log inspection is part of the default dev loop. — refs: `packages/core/runtime-server/src/mlxr/core/server/logging.py`, `docs/dev-harness.md`
- (2026-03-06) [SKILLS] The repo-owned specialized overlays live under `.agents/skills/`: `validation` for generic runtime validation and `ltx-fidelity-debugging` for real-weight LTX fidelity bring-up. They are narrow by design and not replacements for the common startup flow. `.codex/` is reserved for project-local Codex config, not skill bodies. — refs: `.agents/skills/validation/SKILL.md`, `.agents/skills/ltx-fidelity-debugging/SKILL.md`, `AGENTS.md`
- (2026-03-06) [COVERAGE] `verify` enforces an `85%` package coverage floor via `coverage.py`; the floor applies to `packages/`, not repo scripts. — refs: `pyproject.toml`, `scripts/dev.py`
- (2026-03-06) [INSPECTION] Family preflight inspection now runs from `ResolvedSource` without provider fetch; selective provider fetch happens at conversion time through the family adapter’s fetch-policy seam. Hugging Face provenance also preserves `remote_code_approved` from source policy. — refs: `packages/core/runtime-core/src/mlxr/core/runtime/catalog.py`, `packages/core/runtime-core/src/mlxr/core/runtime/contracts.py`, `packages/core/runtime-core/src/mlxr/core/runtime/providers.py`, `packages/families/ltx/src/mlxr/families/ltx/adapter.py`
- (2026-03-06) [ARTIFACTS] LTX Phase B now uses a family-generic multi-source conversion contract: `ArtifactConversionRequest` accepts exactly one of `source_id` or `source_bindings`, portable artifacts can carry typed `components`, and the first truthful LTX slice materializes copied payloads for `checkpoint`, `spatial_upsampler`, and `text_encoder` under `artifacts-portable/.../payload/`. — refs: `packages/core/shared-schemas/src/mlxr/core/schemas/models.py`, `packages/core/runtime-core/src/mlxr/core/runtime/catalog.py`, `packages/families/ltx/src/mlxr/families/ltx/adapter.py`, `docs/research/07-ltx-integration-seams.md`
- (2026-03-06) [PROMPT-ENCODE] The next truthful LTX execution seam is now real: artifactized LTX models lazily create a strict-local MLX Gemma prompt encoder at `prompt_encode`, store prompt context in worker-local runtime state, and keep denoise/generate scaffolded until a later slice. Job inputs with non-empty `negative_prompt` now fail clearly for the fast path instead of being ignored. — refs: `packages/families/ltx/src/mlxr/families/ltx/prompt_encoding.py`, `packages/families/ltx/src/mlxr/families/ltx/adapter.py`, `packages/families/ltx/README.md`
- (2026-03-06) [PROMPT-ENCODE-HARNESS] The typed public prompt-encode seam now lives in `prompt_encoding.py`, while the large MLX implementation is isolated in `_prompt_encoding_backend.py` and omitted from repo coverage until it has real asset-backed tests. API job tests patch `mlxr.core.server.jobs._job_process_context()` to an in-process threaded context so prompt-encoder patches survive worker execution without changing production spawn behavior. — refs: `packages/families/ltx/src/mlxr/families/ltx/prompt_encoding.py`, `packages/families/ltx/src/mlxr/families/ltx/_prompt_encoding_backend.py`, `tests/runtime_test_support.py`, `pyproject.toml`
- (2026-03-06) [LTX-EXECUTION] The first non-placeholder LTX runtime path now resolves imported image handles inside the worker, emits per-stage timing plus MLX memory telemetry, narrows truthful fast-path outputs to runtime-managed `mp4` video, and generates encoded preview video artifacts without claiming checkpoint-faithful denoise yet. — refs: `packages/families/ltx/src/mlxr/families/ltx/adapter.py`, `packages/families/ltx/src/mlxr/families/ltx/generation.py`, `packages/core/runtime-server/src/mlxr/core/server/worker.py`, `docs/research/07-ltx-integration-seams.md`
- (2026-03-06) [HF-BENCHMARK] Hugging Face-backed benchmark runs should isolate download caches with `HF_HUB_CACHE` and `HF_XET_CACHE` instead of overriding `HF_HOME`; `HF_HOME` also carries the saved `hf auth login` token, so replacing it can break gated-model access. On macOS, benchmark UDS sockets should use a short explicit path because deep runtime-home paths can exceed the `AF_UNIX` limit. — refs: `packages/clients/runtime-cli/src/mlxr/clients/cli/benchmark_ltx.py`, `packages/core/runtime-server/src/mlxr/core/server/__main__.py`, `benchmarks/README.md`
- (2026-03-06) [LTX-PROMPT-V2] The current upstream `LTX-2.3` checkpoint uses the Gemma prompt V2 path, not the old single `aggregate_embed` path. The repo-owned prompt backend now needs to read checkpoint config, load `video_aggregate_embed` / `audio_aggregate_embed`, and honor the current connector config (`32x128` video heads, `32x64` audio heads, `8` layers, split rope, float64 rope, gated attention). — refs: `packages/families/ltx/src/mlxr/families/ltx/_prompt_encoding_backend.py`, `references/official/LTX-2/packages/ltx-core/src/ltx_core/text_encoders/gemma/encoders/encoder_configurator.py`, `references/official/LTX-2/packages/ltx-core/src/ltx_core/text_encoders/gemma/embeddings_connector.py`
- (2026-03-07) [LTX-GENERATION-2.3] The current `LTX-2.3` bridge cannot rely on the ecosystem `mlx-video` defaults as-is. The repo-owned generation path now patches the donor transformer for `caption_proj_before_connector`, gated attention, and cross-attention AdaLN, and it uses config-driven local loaders for the video decoder and spatial upsampler because the upstream `2.3` checkpoint metadata carries a newer 9-block VAE decoder shape and an `upsampler.0.*` naming layout the donor loaders do not fully match. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend.py`, `references/ecosystem/mlx-video/mlx_video/models/ltx/ltx.py`, `references/ecosystem/mlx-video/mlx_video/models/ltx/video_vae/decoder.py`, `references/ecosystem/mlx-video/mlx_video/models/ltx/upsampler.py`
- (2026-03-07) [LTX-REAL-SMOKE] The truthful distilled two-stage `LTX-2.3` fast path now completes direct artifact-backed T2V and I2V smoke runs against real upstream weights. The remaining execution gap is no longer “preview versus real generation”; it is recommended-resolution, HQ-profile, and broader feature validation. Because the MLX generation bridge is large and asset-backed, `_generation_backend.py` is temporarily omitted from coverage like `_prompt_encoding_backend.py` until it has dedicated asset-backed test coverage. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend.py`, `packages/families/ltx/README.md`, `pyproject.toml`
- (2026-03-07) [LTX-FIDELITY-GUARDRAILS] The `LTX-2.3` bridge now treats prompt-to-transformer shape/config drift, 22B gated-attention and cross-attention AdaLN feature flags, VAE `mean-of-means` / `std-of-means`, and x2 upsampler key-layout support as fail-closed compatibility boundaries. The prompt encoder now prefers dedicated connector metadata when present and returns an explicit post-connector contract so generation stops silently reshaping mismatched context. — refs: `packages/families/ltx/src/mlxr/families/ltx/prompt_encoding.py`, `packages/families/ltx/src/mlxr/families/ltx/_prompt_encoding_backend.py`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend.py`, `tests/test_runtime_units.py`
- (2026-03-07) [LTX-AV-SMOKE-SAFETY] Real local `LTX-2.3` AV smokes on a `64 GB` Mac can spike into watchdog-danger territory if the Gemma prompt encoder stays resident while the 22B audio-video transformer loads. For manual diagnostics, encode the prompt, close the prompt encoder, clear cache, and only then load/generate. If a manual smoke is interrupted, check for orphaned `uv run python` / `scripts/ltx_debug_smoke.py` processes before launching another run. — refs: `packages/families/ltx/src/mlxr/families/ltx/adapter.py`, `scripts/ltx_debug_smoke.py`, `/Library/Logs/DiagnosticReports/Retired/panic-full-2026-03-07-145930.0002.panic`
- (2026-03-07) [LTX-FIDELITY-WORKFLOW] The canonical LTX fidelity-debug method is now `scripts/ltx_debug_smoke.py` plus `docs/research/10-ltx-fidelity-debug-playbook.md` and `.agents/skills/ltx-fidelity-debugging/`. Use fixed-seed real-weight runs, clean lifecycle, detached execution for long smokes, and stage-local `stage1` / `post_x2` / `final` snapshots to decide the next suspect. `256x160 / 17f` is the first meaningful semantic gate, and an ambiguous quadruped does not count as dog success. — refs: `scripts/ltx_debug_smoke.py`, `docs/research/10-ltx-fidelity-debug-playbook.md`, `.agents/skills/ltx-fidelity-debugging/SKILL.md`
- (2026-03-07) [LTX-DOG-LADDER] After restoring AV cross-timestep prep to use `modality.timesteps` and tightening the prompt-side audio fail-closed path, the canonical fixed-seed dog prompt now clears the first visual gates on the real Hugging Face-converted artifact root: `384x224 / 17f / 24fps` produces a clear dog in a park, and `768x512 / 33f / 24fps` produces a coherent dog scene across `frame_0001`, `frame_mid`, and `frame_last`. The canonical review bundles are under `tmp/manual-runs/20260307T174355Z-dog-visual-gate-after-av-stage1-fix/` and `tmp/manual-runs/20260307T174542Z-dog-coherence-after-av-stage1-fix/`. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend.py`, `packages/families/ltx/src/mlxr/families/ltx/_prompt_encoding_backend.py`, `tmp/manual-runs/20260307T174355Z-dog-visual-gate-after-av-stage1-fix/run_manifest.json`, `tmp/manual-runs/20260307T174542Z-dog-coherence-after-av-stage1-fix/run_manifest.json`
- (2026-03-07) [LTX-AUDIO-EXPORT] The real `LTX-2.3` checkpoint currently used by `MLXR` already carries `audio_vae.*` and `vocoder.*` weights inside the core checkpoint payload. The repo-owned AV bridge now decodes that audio branch and exports it as muxed `mp4` or standalone `wav`, and the first truthful `video.condition.audio` slice now preserves a resolved runtime audio handle through the output path instead of silently ignoring it. The authoritative surface map lives in `docs/research/11-ltx-capability-matrix.md`. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend.py`, `packages/families/ltx/src/mlxr/families/ltx/adapter.py`, `packages/families/ltx/src/mlxr/families/ltx/workflows.py`, `docs/research/11-ltx-capability-matrix.md`
- (2026-03-08) [LTX-AUDIO-FIDELITY] The real `LTX-2.3` checkpoint now runs through a repo-owned MLX BWE wrapper on top of the `AMP1` / `SnakeBeta` / no-tanh / no-bias base vocoder. The wrapper must treat repo-owned `AudioVocoder` waveforms as `[batch, channels, time]`, not `[batch, time, channels]`, and it must load the checkpoint's `vocoder.bwe_generator.*`, `vocoder.mel_stft.mel_basis`, and `vocoder.mel_stft.stft_fn.*` buffers fail-closed. The first BWE-enabled visual-gate receipt is `tmp/manual-runs/20260308T014947Z-dog-bwe-visual-gate-check/`. — refs: `packages/families/ltx/src/mlxr/families/ltx/_audio_bwe.py`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/video_stack.py`, `docs/research/11-ltx-capability-matrix.md`
- (2026-03-08) [SHOWCASE-GATING] `scripts/ltx_showcase_generate.py` is now the repo-owned showcase runner. It uses the shared LTX prompt shaper, saves `ffprobe.json`, and treats Gemini XML review as a promotion gate rather than just a descriptive aid. The first 10-second natural-audio dog showcase receipt now clears that stronger bar after the repo-owned negative-prompt plus rescaled-CFG slice: Gemini identifies a golden retriever with its owner in a park and classifies the audio as barking plus natural ambience. Natural-scene realism is still not solved broadly; treat this as the first scene-level success, not as blanket proof for all natural scenes. — refs: `scripts/ltx_showcase_generate.py`, `packages/families/ltx/src/mlxr/families/ltx/prompting.py`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/sampling.py`, `docs/research/12-ltx-prompting-guide.md`, `docs/research/13-ltx-showcase-execution-plan.md`, `tmp/showcase-runs/20260308T034004Z-showcase-dog-park-natural/gemini-review/gemini-review.json`
- (2026-03-08) [GEMINI-AUDIO-REVIEW] `scripts/gemini_describe_video.py` now stages a review WAV alongside the MP4 when the clip has audio and asks Gemini to review both in one XML response. The stronger review path is good enough to catch real semantic failures and real improvements: it previously confirmed music-like drift on the early dog, nature, and vintage natural-scene receipts, and it now confirms the improved 10-second dog receipt as a visual/audio match. Use the exact receipt path in repo truth instead of assuming one natural-scene result generalizes to the rest. — refs: `scripts/gemini_describe_video.py`, `docs/research/10-ltx-fidelity-debug-playbook.md`, `tmp/showcase-runs/20260308T022707Z-showcase-dog-park-natural/gemini-review-audio-aware/gemini-review.json`, `tmp/showcase-runs/20260308T024917Z-showcase-nature-documentary/gemini-review-audio-aware/gemini-review.json`, `tmp/showcase-runs/20260308T025209Z-showcase-vintage-old-school/gemini-review-audio-aware/gemini-review.json`, `tmp/showcase-runs/20260308T034004Z-showcase-dog-park-natural/gemini-review/gemini-review.json`
- (2026-03-08) [NEGATIVE-GUIDANCE] The current text-first natural-audio improvement path is family-local to LTX. The repo-owned prompt shaper now derives a negative prompt for `natural_audio` / `no_music`, the prompt encoder carries positive and negative contexts together, and the distilled AV path applies rescaled CFG separately to video and audio. Raw CFG without rescale over-pushed the audio into ringing artifacts; the rescaled CFG slice is the one that cleared the 10-second dog natural-audio review. — refs: `packages/families/ltx/src/mlxr/families/ltx/prompting.py`, `packages/families/ltx/src/mlxr/families/ltx/_prompt_encoding_backend/encoder.py`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/sampling.py`, `tmp/showcase-runs/20260308T033618Z-showcase-dog-park-natural/run_manifest.json`, `tmp/showcase-runs/20260308T034004Z-showcase-dog-park-natural/run_manifest.json`
- (2026-03-08) [NATURAL-AUDIO-SCOPE] The newer negative-guidance/rescaled-CFG path is not a blanket fix for natural-audio realism. It genuinely recovers the 10-second dog scene, but the rerun `nature_documentary` and `vintage_old_school` scenes on the same path still come back as visual matches with music-like audio. Treat the current public story as the best-available mixed five-pack, not as solved natural-scene realism. — refs: `tmp/showcase-runs/20260308T034004Z-showcase-dog-park-natural/gemini-review/gemini-review.json`, `tmp/showcase-runs/20260308T035528Z-showcase-nature-documentary/gemini-review/gemini-review.json`, `tmp/showcase-runs/20260308T035838Z-showcase-vintage-old-school/gemini-review/gemini-review.json`, `docs/research/14-first-showcase-pack.md`
- (2026-03-08) [SCENARIO-AUDIO-SHAPING] `scripts/ltx_showcase_generate.py` now feeds fixture-level `audio_intent` into the shared prompt shaper and passes `style_family` through to style-aware anti-music negatives. That newer runner-only slice clearly rescues `heron_marsh_documentary` at the 10-second `384x224 / 241f` rung, while `cat_kitchen_natural` and the rerun `vintage_laundromat` still drift toward music. Separate current repo receipts also show no-music wins for `basketball_court_dusk` and `cafe_sidewalk_human`, but do not over-attribute those older wins to this exact shaping change. Use this as the current dividing line: strong foreground diegetic anchors are working much better than passive ambience-led scenes. — refs: `scripts/ltx_showcase_generate.py`, `packages/families/ltx/src/mlxr/families/ltx/prompting.py`, `tests/test_ltx_prompting.py`, `tests/test_ltx_showcase_generate.py`, `tmp/showcase-runs/showcase-summary-20260308T092838Z.json`, `tmp/showcase-runs/showcase-summary-20260308T093536Z.json`, `tmp/showcase-runs/showcase-summary-20260308T094420Z.json`, `tmp/showcase-runs/showcase-summary-20260308T094012Z.json`, `tmp/showcase-runs/showcase-summary-20260308T094732Z.json`
- (2026-03-08) [COMBINED-A2V-I2V] The current `ltx` workflow strategy already preserves image references when `video.condition.audio` is selected, which matches the official upstream direction for combined text + image + audio inputs. This combined slice is now pinned by runtime workflow tests and is a valid next promotion target after conditioned-scene quality improves. — refs: `packages/families/ltx/src/mlxr/families/ltx/workflows.py`, `tests/test_runtime_workflows.py`, `references/official/LTX-2/packages/ltx-pipelines/src/ltx_pipelines/a2vid_two_stage.py`
- (2026-03-08) [MP4-EXPORT] The LTX MP4 export path now defaults to H.264 instead of legacy `mpeg4`. Prefer `h264_videotoolbox` on Apple Silicon, fall back to `libx264`, keep `yuv420p`, and mux AAC audio into the final container. The tiny runtime receipt `tmp/manual-runs/20260308T073808Z-h264-export-smoke/` is the first explicit proof that the current runtime-managed output now lands as `codec_name=h264` with `codec_tag_string=avc1`. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/outputs.py`, `tests/test_runtime_units.py`, `tmp/manual-runs/20260308T073808Z-h264-export-smoke/run_manifest.json`
- (2026-03-08) [PROMPT-SCENARIOS] The canonical reusable LTX prompt scenario pack now lives in `tests/fixtures/ltx/prompt_scenarios.json`, and `scripts/ltx_showcase_generate.py` loads that fixture instead of maintaining a second hardcoded scene table. Treat the fixture as code-loaded validation data: expand it deliberately, and use explicit accepted subject/scene aliases when Gemini review needs semantically broader matching than one literal phrase. — refs: `tests/fixtures/ltx/prompt_scenarios.json`, `scripts/ltx_showcase_generate.py`, `tests/test_ltx_showcase_generate.py`
- (2026-03-08) [AUDIO-LATENT-CONTRACT] The current artifactized LTX bundle may not include `audio_vae/config.json`, so audio latent mel bins must be derived from the transformer contract, not guessed from missing audio-VAE defaults. For the current `LTX-2.3` fast path, `audio_in_channels=128` and `audio_encoder.latent_channels=8` imply `audio_latent_mel_bins=16`. If that inference is wrong, the real runtime fails in the transformer audio preprocessor with a shape mismatch even when the rest of the backend is healthy. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/config.py`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/runtime_helpers.py`, `tests/test_runtime_units.py`, `references/official/LTX-2/packages/ltx-core/README.md`
- (2026-03-08) [AUDIO-DECODER-WEIGHTS] The audio decoder loader must receive raw checkpoint audio weights exactly once. Pre-sanitizing and re-prefixing `audio_vae.*` weights before `_load_runtime_audio_decoder()` causes a second sanitation/remap pass and breaks the decoder conv layout on real artifact-backed runs. The correct seam is: load the unified checkpoint weights once, pass the raw prefixed mapping into the decoder loader, and let the runtime loader own the sanitation. — refs: `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/runtime_helpers.py`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/video_stack.py`, `tests/test_runtime_units.py`
- (2026-03-08) [DONOR-FREE-DISTILLED] The promoted distilled LTX generation engine is now donor-free for `mlx_video` runtime imports. The owned engine now covers latent upsampling, VAE encoder/decoder-side helpers, audio autoencoder/BWE stack, rotary embeddings, AdaLN, transformer preprocessors, attention, transformer blocks, and the top-level `LTXModel` bridge inside the LTX family package. The next donor-removal wave is the Gemma prompt path through `mlx-vlm` / `mlx-lm`, guided by `docs/research/17-multimodal-inference-engine.md`. — refs: `docs/research/16-owned-substrate-migration-plan.md`, `docs/research/19-ltx-compatibility-checklist.md`, `packages/families/ltx/src/mlxr/families/ltx/_generation_backend/transformer_model.py`
- (2026-03-07) [WORKFLOW-LAYER] `MLXR` now has a shared core workflow-planning layer between scheduler, family adapters, and host adapters. Today it owns intent normalization, family-dispatched planning, and conversion into the primitive job API; it does not yet own full stage-graph execution. Families still own truthful workflow templates and stage implementations, and hosts stay thin. — refs: `docs/workflow-orchestration-design.md`, `AGENTS.md`, `docs/agent-native-development.md`
- (2026-03-07) [FAMILY-BRINGUP] The default bring-up pack is now `docs/family-bringup/` plus `.agents/skills/family-bringup/`. New-family work should start from the smallest truthful slice, record source/artifact/workflow/capability contracts explicitly, and complete a fresh-eyes review before commit. — refs: `docs/family-bringup/README.md`, `docs/family-bringup/checklist.md`, `docs/family-bringup/template.md`, `.agents/skills/family-bringup/SKILL.md`
